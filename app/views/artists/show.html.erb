<h1><%= @artist.name %></h1>
<h3>Song count: <%= @artist.song_count if @artist.song_count > 0 %> song</h3>
<ul>
  <% @artist.songs.each do |s| %>
    <li><%= link_to s.title, song_path(s) %></li>
  <% end %>
</ul>